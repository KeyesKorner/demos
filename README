This python program inputs a colon-delimited text file which is output from a test framework. The script produces
an HTML file with the failed test cases highlighted in red.

1. Edit the example_python.py and put your current working directory for the value for testRootDir.
When executed, the python script will input the colon-delimited file named result_file.txt and create a HTML file.
2. execute 
python example_python.py
3. A file will be written named AutomationTestSummary.html. It should look like AutomationTestSummary_base.html.
